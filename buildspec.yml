version: 0.2
env:
  variables:
    DEBIAN_FRONTEND: noninteractive

phases:
  pre_build:
    commands:
      - find ./ -mtime +10950 -print 
  build:
    commands:
      - npm install
      - node_modules/.bin/babel index.js -o index.js
  post_build:
    commands:
      - find ./ -mtime +10950 -print 
